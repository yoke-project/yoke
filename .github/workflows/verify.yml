# Every proposed change runs this repository's own verbs, from a clean checkout.
# Every command is a verb, or a script under ci/.
name: verify

on:
  pull_request:
  push:
    branches: [main]

jobs:
  changes:
    runs-on: ubuntu-24.04
    outputs:
      verify: ${{ steps.select.outputs.verify }}
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0
      - id: select
        run: ci/changed-paths.sh "${{ github.event.pull_request.base.sha || github.event.before }}" | ci/select-jobs.sh | sed 's/$/=true/' >> "$GITHUB_OUTPUT"

  verify:
    needs: changes
    if: needs.changes.outputs.verify == 'true'
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v7
      - uses: extractions/setup-just@v4
        with:
          just-version: 1.58.0
      - uses: actions/setup-go@v7
        with:
          go-version: '1.26'
      - run: just build
      - run: just lint
      - run: just fmt
      - run: just test
      - run: ci/record.sh L1 > record-L1.json
        if: always()
      - run: ci/record.sh L3 > record-L3.json
        if: always()
        env:
          PREDECESSOR: record-L1.json
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: record-L1
          path: record-L1.json
      - uses: actions/upload-artifact@v7
        if: always()
        with:
          name: record-L3
          path: record-L3.json
